pipeline {
    agent {
        docker { image 'node:14-apline'}
    }
    stages {
        stage('Docker node version') {
            steps {
                sh 'node --version'
            }
        }
    }
}
